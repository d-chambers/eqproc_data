# eqproc data

This is a directory of data files for the eqproc package. It is not to be used directly but rather by certain eqproc modules.

Command to ignore index:
git ls-files -z data/ | xargs -0 git update-index --skip-worktree

